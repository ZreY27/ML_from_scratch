# Fichiers Git
.git
.gitignore

# Environnements virtuels et variables d'environnement
venv
.env
.env.local
__pycache__/
build/

# Fichiers de configuration des éditeurs
.vscode
.idea
.DS_Store

# Dossiers de build CMake
cmake-build-debug/
cmake-build-release/

# Binaires compilés par pybind11 (spécifiques à chaque OS, ne pas commiter)
*.so
*.pyd

# Environnement virtuel créé par CLion
.venv/

*.pdf
*.png
*.txt

datasets/

# Modèles entraînés : manifestes JSON (les poids *.txt sont déjà ignorés ci-dessus).
# Modèles locaux à chaque machine -> ne pas committer.
models/*.json

# Logs TensorBoard (générés à chaque entraînement)
runs/